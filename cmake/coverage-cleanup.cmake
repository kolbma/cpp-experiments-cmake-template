file(REMOVE_RECURSE ${CMAKE_BINARY_DIR}/gcov)
file(MAKE_DIRECTORY ${CMAKE_BINARY_DIR}/gcov)
file(GLOB_RECURSE GCOV_FILES ${CMAKE_BINARY_DIR}/*.gcda)
if(GCOV_FILES)
  file(REMOVE ${GCOV_FILES})
endif()
